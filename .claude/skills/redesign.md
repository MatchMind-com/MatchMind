Redesign this component or page so it looks like a real sports product, not a generic AI SaaS template.

Component/page to redesign: $ARGUMENTS

The problem with the current design:
- Looks like every other dark-mode SaaS landing page (blur glows, gradient text, card grids)
- Feels AI-generated / template-based
- Missing sports product energy — should feel like match day, not a tech dashboard

Design direction to aim for:
- **Editorial / sports media feel**: Think The Athletic, bet365's match pages, Opta dashboards — data-dense, confident, purposeful
- **Bold typography**: Larger, more expressive type. Use font weight contrast dramatically.
- **Real data front and center**: Don't bury numbers in small text. Make EV%, odds, AI probability the hero.
- **Raw match-day energy**: Tight spacing, stat rows, scoreboard-style layouts — not soft rounded cards everywhere
- **Color with purpose**: Orange accent for edge/value only (not decoration). Use green for wins, red for losses — like real sports.
- **Asymmetry and tension**: Break the 3-column grid monotony. Use full-width elements, offset layouts, pull quotes.
- **Remove**: generic blur orbs, over-used gradient text spans, "glass morphism" cards for everything, animate-pulse on things that aren't actually live.

When redesigning:
1. First describe what specifically looks "AI/generic" about the current design
2. Propose the new design direction with reasoning
3. Write the new Tailwind JSX code
4. Keep all existing functionality — only change visual presentation
5. Make sure it's still mobile responsive

Stack: Next.js, Tailwind CSS, TypeScript. No extra UI libraries.
