import MemoriesClient from '@/components/dashboard/MemoriesClient'

export const metadata = { title: 'Memory Lane — MatchMind' }

export default function MemoriesPage() {
  return <MemoriesClient />
}
